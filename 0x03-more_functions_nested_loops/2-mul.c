#include "holberton.h"
/**
 * mul - multiplies two integers and returns result
 * @a: first input
 * @b: second input
 *
 * Description - two integers are the parameters.
 * Return: result of two integers
 */
int mul(int a, int b)
{
	return (a * b);
}
