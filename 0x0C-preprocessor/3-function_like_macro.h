#ifndef _FUNCTION_LIKE_MACRO_
#define _FUNCTION_LIKE_MACRO_
/* Define absolute value of x, if x is less than 0 or neg, make it positive */
#define ABS(x) ((x < 0) ? (-x) : (x))
#endif /* _FUNCTION_LIKE_MACRO_ */
