#include "lists.h"
#include <stdio.h>
/**
 * get_nodeint_at_index - a function that returns nth node of a listint_t list
 * @head: a pointer to head of the list
 * @index: index of the node starting at zero
 *
 * Return: nth node
 */
listint_t *get_nodeint_at_index(listint_t **head, unsigned int index)
{
/* index is the index o the node starting at zero */
	index = 0;
/* if the node does not exist, return NULL */

/* returns nth node */
	return (index);
}
