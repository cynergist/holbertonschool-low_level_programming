#include "lists.h"
#include <stdio.h>
/**
 * sum_listint - a function that returns the sum of all data in a listint_t list
 * @head: a pointer to head of the list
 *
 * Return: sum of n
 */
int sum_listint(listint_t *head)
{
/* if list is empty, return 0 */
}
